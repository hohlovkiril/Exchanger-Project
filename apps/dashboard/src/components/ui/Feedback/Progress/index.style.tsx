import { CircularProgress, LinearProgress, styled } from "@mui/material";

export const MuiCircularProgress = styled(CircularProgress)(({ theme }) => ({

}))

export const MuiLinearProgress = styled(LinearProgress)(({ theme }) => ({
  
}))